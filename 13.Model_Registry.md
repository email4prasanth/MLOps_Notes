- ML Lifecycle 
```sh
Model Development --> Model Registry --> Model Deployment --> Monitoring and tracking
```
- update the code replace numpy array index mapping to pandas column name 
```sh
X_train = train_processed_data.drop(columns = ['Potability'], axis=1)
y_train = train_processed_data['Potability']
```
- Infer signature to get the input hypermeters (X) and output parameter (y)
```sh
sign = infer_signature(X_test, random_search.best_estimator_.predict(X_test))
```
- Now check the UI, Once the model is available the click on Register Model and create registry, the version will automatically update. we can move the stage
```sh
None --> Stagging --> Production --> Archive
```