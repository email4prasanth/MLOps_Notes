## Log only best parameters found by RandomizedSearchCV(cross validation)
- use jupiter code create a py file, add randomsearchcv and list the parameter distribution n_estimators, max_depth
1. perform random search and find the best paramenter and fit X_train, y_train.
2. Train the model with best parameters and save the model.
- Now add mlflow requirements
- it is observed that dataframe should be converted to mlflow read mode so use mlflow.data.from_pandas() and mlflow.log_input(df, "name")
- The model details are stored under Model training icon, setting uri to 127.0.0.1:5000 is not required.

## logging all combination of hyperparameters
- set experiment name
- use start_run as parent_run fit in random_search  
- use for loop on cv result **params** then start_run as child_run log the respective params and mean