- First create a sample file **test_file.py ** 
```sh
python test_file.py 
```
- Install check the lint errors
```
pip install flake8
flake8 test_file.py
```
- Now push the code to git
```sh
git init
git status
git add 
git commit -m 'created file'
git remote add origin https://github.com/email4prasanth/ci_mlflow.git
git branch cicd
git checkout cicd
git branch
git push origin cicd
```
- Now create yml file under github workflow
- Use flake8, black, ruff to test the code
- Use different os compatablility with different python version
- Use unittest **test_unit.py** and verify the **example.py** code
```sh
python -m unittest discover
``` 