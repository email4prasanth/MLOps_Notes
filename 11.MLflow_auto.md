- Create py file to get datasets, disable dagshub MLflow Tracking URI
```sh 
mlfow.set_experiment # to give unique name
mlflow.data.from_pandas # use to get the dataframe
mlfow.log_input # use this to get the df logs in the mlfow experiment UI
```
- Once the above is done to auto log mlflow
```sh
mlflow.set_experiment
mlflow.autolog()
# disable all mlflow log metrics
```